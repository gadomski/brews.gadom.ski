services:
  backend:
    command:
      - uv
      - run
      - uvicorn
      - brews.app:create
      - --factory
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
    volumes:
      - ./src:/app/src
